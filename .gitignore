node_modules/
.next/
out/
dist/
coverage/
*.log
.DS_Store
tsconfig.tsbuildinfo

# Local env files (never commit; PostHog key lives here)
.env
.env*.local

# Impeccable runtime state (the design record itself is committed)
.impeccable/live/sessions/
.impeccable/live/roots.json
.impeccable/live/inject-journal.json
